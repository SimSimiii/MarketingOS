# Vendored by buildspec.yml at build time from ../../backend/app. Never
# committed: a checked-in copy is a second definition of every model, and the
# day it drifts is the day the console reports numbers the product disagrees
# with.
/app/
.aws-sam/
